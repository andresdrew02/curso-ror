<!--El atributo data-turbo-frame es para poder renderizar los links a los productos fuera del frame de turbo que hemos creado para que la página sea reactiva-->
<%= link_to product_path(product.id), class: 'product', 'data-turbo-frame':'_top' do %>
  <%= image_tag product.photo, width: '100px' if product.photo.attached? %>
  <h2>
    <%= product.title %>
  </h2>
<%end %>
<span>
  <%= product.price %>$
</span>
