<%= link_to product_path(product.id), class: 'product' do %>
  <%= image_tag product.photo, width: '100px' if product.photo.attached? %>
  <h2>
    <%= product.title %>
  </h2>
<%end %>
<span>
  <%= product.price %>$
</span>
